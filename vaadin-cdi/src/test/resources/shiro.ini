[users]
# username = password, role...
admin = admin, admin, viewer
demo = demo, viewer

# Implicit roles so we don't need a [roles] section.

# In a real application, one would typically using permissions rather than
# roles for authorization.